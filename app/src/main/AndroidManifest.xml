<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imaec.triplan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TriPlanApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Triplan">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.calendar.CalendarActivity"/>
        <activity android:name=".ui.plan.PlanDetailActivity"/>
        <activity android:name=".ui.plan.write.PlanWriteActivity"/>
        <activity android:name=".ui.plan.more.PlanMoreActivity"/>
        <activity android:name=".ui.place.PlaceMoreActivity"/>
        <activity android:name=".ui.writeplace.WritePlaceActivity"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".ui.select.category.SelectCategoryActivity"/>
        <activity android:name=".ui.select.city.SelectCityActivity"/>
        <activity android:name=".ui.select.place.SelectPlaceActivity"/>
        <activity android:name=".ui.writeplace.searchaddress.SearchAddressActivity"/>
        <activity android:name=".ui.category.CategoryManagementActivity"/>
        <activity android:name=".ui.city.CityManagementActivity"/>
    </application>
</manifest>
